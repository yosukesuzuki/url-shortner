import logging


"""
from random import shuffle


def generate_combination(list):
    shuffle(list)
    shuffled = list
    combination = []
    for i, val in enumerate(shuffled):
        logging.info(i, val)
        if i < (len(list) - 1):
            combination.append({'from': shuffled[i], 'to': shuffled[i+1]})
        else:
            combination.append({'from': shuffled[i], 'to': shuffled[0]})
    shuffle(combination)
    return combination
"""
